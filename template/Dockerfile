# docker.io OCI image if no domain is given
FROM <IMAGE>[:<version>]

# RUN commands are only executed during build
RUN <COMMANDS>

COPY <SRC> <DEST>

# command executed at runtime usualy to start the app
CMD [ "<start>", "<app>" ]